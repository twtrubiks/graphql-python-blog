mutation FollowUser($userId: ID!) {
  followUser(userId: $userId) {
    success
    message
    follow {
      id
      followed {
        id
        followersCount
        isFollowedByMe
      }
    }
  }
}
